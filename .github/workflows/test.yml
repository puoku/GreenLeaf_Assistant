name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      BOT_TOKEN: test-token

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: greenleaf_bot_project_v2/requirements.txt

      - name: Install dependencies
        working-directory: greenleaf_bot_project_v2
        run: pip install -r requirements.txt

      - name: Run tests
        working-directory: greenleaf_bot_project_v2
        run: pytest
